import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Inventario Patrimonial B',
  webDir: 'build'
};

export default config;
